# Common puppeteer

## Requirements

1. Node.js 14+
2. Installed Yarn (`npm i -g yarn`)

## Installation

```shell
$ yarn install
```

## Configuration

App can be configured using environment variables or using `.env` file

```dotenv

```

## Usage
